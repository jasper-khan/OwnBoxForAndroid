# OwnBox v2.8.28-preview 预览版

- 设置 → 核心设置：「并发拨号（多 IP 同时连接）」更名为「TCP 并发」，说明文案与 mihomo 文档对齐：启用 TCP 并发连接，将会使用 dns 解析出的所有 IP 地址进行连接，使用第一个成功的连接。
- 仅调整界面文案，功能和内核不变：内核仍为 `v1.15.0-alpha.8-ownbox.1`，v2.8.26 的应用分流（user_id）修复与 v2.8.27 的崩溃修复一并包含。
