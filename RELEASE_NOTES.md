## 变更

- 「清理缓存」在清理应用缓存文件的同时，调用内核原生接口清除 DNS 缓存与 FakeIP 缓存，清理立即生效。
- FakeIP 缓存清理同时覆盖内存映射与 `cache.db` 持久化映射；同步更新功能说明文案。

## 注意事项

- 清理 FakeIP 缓存后，应用已缓存的 `198.18.x.x` 地址将失效，需重新解析域名后恢复连接。
- 内核未运行或 Clash API 未启用时，跳过 DNS 与 FakeIP 缓存清理，仅执行文件清理。
